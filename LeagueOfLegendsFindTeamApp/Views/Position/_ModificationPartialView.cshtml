@model LeagueOfLegendsFindTeamApp.Models.DatabaseModels.Position

<div class="card-header">
    <strong>Modification @Html.Name(Model.Name) position</strong>
</div>
<div class="card-body">
    @{
        using (Ajax.BeginForm("ModifyPosition", "Position",
            new AjaxOptions { HttpMethod = "post", UpdateTargetId = "PositionCard", InsertionMode = InsertionMode.Replace, OnSuccess = "LoadTable()" },
            new { id = "PositionCard" }))
        {
            @Html.HiddenFor(m => m.PositionId)

            @Html.LabelFor(m => m.Name)
            @Html.EditorFor(m => m.Name, new { htmlAttributes = new { @class = "form-control" }, })



            <button type="submit" class="btn btn-sm btn-success float-right mt-5"><i class="zmdi zmdi-upload mr-2" id="BtnUpdate" name="BtnUpdate"></i>Update</button>
        }

        <button type="button" class="btn btn-sm btn-secondary float-left mt-5" name="BtnReturn" id="BtnReturn" onclick="GoToTableForm()"><i class="zmdi zmdi-long-arrow-return mr-2"></i>Return</button>

        <div style="text-align: center;">
            <button type="button" class="btn btn-sm btn-danger mt-5" name="BtnReturn" id="btnRemove" onclick="RemovePosition(@Html.Raw(Model.PositionId))"><i class="zmdi zmdi-delete mr-2"></i>Remove</button>
        </div>

    }
</div>


